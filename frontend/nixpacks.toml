# frontend/nixpacks.toml
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve@latest -s dist/endpoint-project -l tcp://0.0.0.0:$PORT"
